[tool.astropy-bot]
[tool.astropy-bot.changelog_checker]
filename = "CHANGES.rst"

[build-system]

requires = ["setuptools",
            "setuptools_scm",
            "wheel"]

build-backend = 'setuptools.build_meta'
